version: '3.8'

services:
  app:
    build: .
    container_name: ${PROJECT_NAME}
    ports:
      - "${APP_PORT}:80"

  redis:
    image: redis
    container_name: devops-redis
    ports:
        - "${REDIS_PORT}:6379"